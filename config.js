/* =====================================================================
 * 应用方配置文件（内置翻译服务）
 *
 * 这里填的是「应用方自己的 API Key」，司机打开网页就能直接翻译，
 * 不需要自己去申请 Key。司机在设置里填了自己的 Key 时，优先用司机自己的。
 *
 * 使用前请确认三件事：
 * 1. 服务商允许网页直接调用，否则会出现跨域失败，需要改成服务端代理；
 * 2. 在服务商后台设置用量上限与费用提醒；
 * 3. 这个文件会随网页一起发给所有使用者，任何拿到链接的人都能看到 Key，
 *    所以只适合给自己人和种子司机用，不要公开分享链接。
 *
 * 安全建议：正式对外使用前，把翻译请求改成走自己的服务端代理，
 * 前端不再保存 Key，详见 PRD 第 17.5 节。
 * ===================================================================== */
window.APP_CONFIG = {
  builtinEngine: {
    // 关掉这一项，所有设备就只能用离线表达库，或由司机自己填 Key
    enabled: true,

    // 服务商接口（OpenAI 兼容格式）
    baseUrl: "https://api.deepseek.com/v1",

    // deepseek-flash：快（约 1 秒）又便宜，翻译口语自然，日常用它就够
    // deepseek-v4-pro：更细致一点，但慢一倍，适合不着急的场景
    model: "deepseek-flash",

    // 应用方的 API Key（DeepSeek）。如果这个链接被公开传播、费用异常，
    // 请立刻到 DeepSeek 后台吊销并换一个，只改这一行即可。
    apiKey: "sk-917da6c90d4d4fbca471472bd181205e",

    // 每台设备每天最多用内置 Key 翻译多少次，超出后自动回落离线表达库
    dailyLimitPerDevice: 300
  }
};
